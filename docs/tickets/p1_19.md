# P1-19 — Create Container (manual)

> Phase: **P1 — Account Lifecycle & Provisioning** · Priority: **P0** · Estimate: **M** · Depends on: P1-04, P1-05

## Context

Fase inti operasional: operator menambah akun dari UI → BE provisioning container → worker login headful interaktif → session tersimpan. Tanpa akun aktif, tidak ada scrape/action.

Tiket ini: Create Container (manual).

## Scope

API `POST /containers` (pilih platform, `source=MANUAL`) + UI "Create Container" → `Worker` RUNNING → reconcile

## Technical Notes

Endpoint `POST /containers` + UI "Create Container" (pilih platform). Insert `Worker` `source=MANUAL`, `desiredState=RUNNING` → `enqueue reconcile`. Ini jalur default saat fleet kosong (tidak ada worker sampai user bikin). Uji: buat container tanpa akun → pod+PVC jadi, tidak auto-delete.

Invarian kunci: 1 container = 1 device = N akun, maks 1 per platform (@@unique([workerId, platform])). Channel: queue:action:<workerId> + control-<workerId>. Commit DB SEBELUM publish/enqueue.

## Acceptance Criteria

- [ ] User buat container saat fleet kosong
- [ ] pod+PVC terbuat
- [ ] `MANUAL` 0 akun tidak auto-delete

## Test Plan

Integration akun dummy end-to-end; envtest/kind untuk K8s; unit untuk reconciler diff & packing matrix.

## Definition of Done

- [ ] PR ≤ 400 LOC, review ≥ 1 approver.
- [ ] Lint + typecheck + test hijau (CI).
- [ ] Docs terdampak di-update (OpenAPI/Storybook/runbook).
- [ ] AC semua terpenuhi. Tiket ditutup.

## References

- `../DEVELOPMENT_RULE.md`, `../DEVELOPMENT_PHASE.md`, `../TICKETS.md`.
- Phase doc: `../DEVELOPMENT_PHASE.md` → P1 (Account Lifecycle & Provisioning).
