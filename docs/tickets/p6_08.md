# P6-08 — Bulk import accounts

> Phase: **P6 — Prototype Parity (Dashboard Productization II)** · Priority: **P1** · Estimate: **M** · Depends on: P6-07

## Context

Audit `../development-analyst/README.md` menemukan 19 layar prototype vs 7 route terbangun: 5 halaman MISSING (login, add-account, bulk-import, audit, settings) dan 12 PARTIAL (termasuk layout + tema). P6 menutup gap: setiap layar prototype punya route yang konsisten, light+dark jalan, dan setiap kontrol action menjalankan dummy process yang terlihat.

Tiket ini: Bulk import accounts.

## Scope

Import CSV/paste banyak akun sekaligus; validasi per baris

## Technical Notes

UI + wiring saja — tidak bangun scraper/adapter/platform baru. Konsumsi API yang ada; jika API belum ada, halaman render empty state eksplisit, bukan data palsu. Analitik = akun official (read-only, sumber 3rd-party), worker tidak masuk analitik. Kontrak layout dari `docs/prototype/shell.js`: header (h-14) → main (p-6) → #page (space-y-4), tanpa mt-* manual.

## Acceptance Criteria

- [ ] Import multi-akun
- [ ] error per baris jelas

## Test Plan

Smoke render setiap route baru (light+dark, no console error); side-by-side review vs `docs/prototype/`; make ci hijau.

## Definition of Done

- [ ] PR ≤ 400 LOC, review ≥ 1 approver.
- [ ] Lint + typecheck + test hijau (CI).
- [ ] Docs terdampak di-update (OpenAPI/Storybook/runbook).
- [ ] AC semua terpenuhi. Tiket ditutup.

## References

- `../DEVELOPMENT_RULE.md`, `../DEVELOPMENT_PHASE.md`, `../TICKETS.md`.
- Phase doc: `../DEVELOPMENT_PHASE.md` → P6 (Prototype Parity (Dashboard Productization II)).
