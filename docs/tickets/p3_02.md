# P3-02 — Template engine

> Phase: **P3 — Action Engine (Playwright)** · Priority: **P0** · Estimate: **M** · Depends on: P3-01

## Context

Fase auto-engagement (paling berisiko ban): worker eksekusi comment & like sequential lintas akun dengan template + verifikasi ground truth.

Tiket ini: Template engine.

## Scope

Template pool + var `{topic}{product}{handle}` + random + dedupe 7 hari

## Technical Notes

Invarian: worker hanya SUBSCRIBE Redis + POST callback, tidak pernah sentuh DB. Verdict = baris upsert UNIQUE (actionJobId, attempt). Tidak ada SUCCESS tanpa verifikasi. Cooldown gate 60 dtk per (akun, target).

## Acceptance Criteria

- [ ] Pick random
- [ ] dedupe per target 7 hari

## Test Plan

Unit adapter dengan DOM fixture; integration akun dummy; contract test callback ke handler BE (replay fixture).

## Definition of Done

- [ ] PR ≤ 400 LOC, review ≥ 1 approver.
- [ ] Lint + typecheck + test hijau (CI).
- [ ] Docs terdampak di-update (OpenAPI/Storybook/runbook).
- [ ] AC semua terpenuhi. Tiket ditutup.

## References

- `../DEVELOPMENT_RULE.md`, `../DEVELOPMENT_PHASE.md`, `../TICKETS.md`.
- Phase doc: `../DEVELOPMENT_PHASE.md` → P3 (Action Engine (Playwright)).
