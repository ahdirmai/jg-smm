# P2-10 — ERD akun resmi

> Phase: **P2 — Scrape (IG + Threads) & Monitoring** · Priority: **P0** · Estimate: **M** · Depends on: P0-04

## Context

Fase data: scrape via Apify actor (K8s Job ephemeral), ingest raw → MinIO → Postgres, metric time-series, alert. **Plus monitoring akun resmi (Official Accounts)** yang read-only: metrik dari 3rd-party provider, terpisah dari jalur worker/scrape.

Tiket ini: ERD akun resmi.

## Scope

`OfficialAccount`, `AnalyticsSnapshot`, `AnalyticsMention`, `AnalyticsIngestRun` + enum

## Technical Notes

Model baru **terpisah** dari `Account`: `OfficialAccount` (tanpa kredensial), `AnalyticsSnapshot` (time-series, `metrics` JSONB untuk metrik platform-spesifik + kolom scalar ter-index), `AnalyticsMention` (unique `platform+externalId`), `AnalyticsIngestRun` (audit, mirror `ProvisionLog`). Ikuti DDL convention yang sudah live (`uuid` PK `gen_random_uuid()`, `timestamptz`, `snake_case`). Snapshot = hypertable Timescale, retention 90h/1thn.

Rate limit dipaksa scheduler BE, bukan di dalam job. Raw payload di MinIO, normalisasi idempotent (upsert by externalId). TimescaleDB untuk MetricSnapshot. **OfficialAccount ≠ Account** (worker): akun resmi tanpa kredensial/login/action, hanya subjek analitik. Provider diakses lewat interface `Provider` (provider-agnostic) — jangan hardcode vendor. Ingest gagal tidak boleh memblokir jalur action/scrape; tampilkan snapshot terakhir + badge `stale`.

## Acceptance Criteria

- [ ] Migration bersih
- [ ] unique `(platform,handle)`
- [ ] hypertable `AnalyticsSnapshot`

## Test Plan

Integration mock actor; unit normalizer per platform; re-run ingest tidak duplikat. Unit provider adapter pakai fake HTTP server; unit ingestor: idempotent per (account, ts), `IngestRun` transisi status, provider error → PARTIAL/FAILED.

## Definition of Done

- [ ] PR ≤ 400 LOC, review ≥ 1 approver.
- [ ] Lint + typecheck + test hijau (CI).
- [ ] Docs terdampak di-update (OpenAPI/Storybook/runbook).
- [ ] AC semua terpenuhi. Tiket ditutup.

## References

- `../DEVELOPMENT_RULE.md`, `../DEVELOPMENT_PHASE.md`, `../TICKETS.md`.
- Phase doc: `../DEVELOPMENT_PHASE.md` → P2 (Scrape (IG + Threads) & Monitoring).
