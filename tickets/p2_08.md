# P2-08 — Monitoring dashboard FE

> Phase: **P2 — Scrape (IG + Threads) & Monitoring** · Priority: **P1** · Estimate: **M** · Depends on: P2-05, P1-17

## Context

Fase data: scrape via Apify actor (K8s Job ephemeral), ingest raw → MinIO → Postgres, metric time-series, alert.

Tiket ini: Monitoring dashboard FE.

## Scope

Chart metric per akun/post + sparkline + filter waktu

## Technical Notes

Rate limit dipaksa scheduler BE, bukan di dalam job. Raw payload di MinIO, normalisasi idempotent (upsert by externalId). TimescaleDB untuk MetricSnapshot.

## Acceptance Criteria

- [ ] Data live
- [ ] p95 load < 1.5 dtk

## Test Plan

Integration mock actor; unit normalizer per platform; re-run ingest tidak duplikat.

## Definition of Done

- [ ] PR ≤ 400 LOC, review ≥ 1 approver.
- [ ] Lint + typecheck + test hijau (CI).
- [ ] Docs terdampak di-update (OpenAPI/Storybook/runbook).
- [ ] AC semua terpenuhi. Tiket ditutup.

## References

- `DEVELOPMENT_RULE.md`, `DEVELOPMENT_PHASE.md`, `TICKETS.md`.
- Phase doc: `DEVELOPMENT_PHASE.md` → P2 (Scrape (IG + Threads) & Monitoring).
